package com.ninjadroid.app.webServices.callbacks;

import com.ninjadroid.app.utils.containers.ProfileContainer;

public interface VolleyProfileCallback {
    void onSuccess(ProfileContainer profile);
}